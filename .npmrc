; @capacitor-firebase/authentication@8 declares an optional peer of firebase@^12
; while this app is on firebase@10 — a pre-existing conflict that npm v7+
; hard-fails on during fresh installs (e.g. Vercel builds). Legacy resolution
; matches how the project has always been installed locally.
legacy-peer-deps=true
